# Runtime state — cloned repos, sessions, worktrees, role/auth/cache files.
# 16K+ files, mostly external repo content; not Clack source.
data/

# Skill-local working directories
.systematic/

# Migration project scratch
openspec/changes/add-claude-run-handle/

# Coverage / test output (if generated)
coverage/

# Lockfiles graphify already skips by default — listing here for clarity
package-lock.json
