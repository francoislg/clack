# Ignore everything by default
*

# Source code (for builder stage)
!src/
!tsconfig.json
!tsconfig.build.json
!package.json
!package-lock.json

# Default configuration (instruction templates)
!data/default_configuration/

# Example configs
!data/config.example.json
!data/mcp.example.json
!data/auth/slack.example.json
!data/auth/.env.example
!data/auth/github.example.json

# Dockerfile itself (needed by BuildKit)
!Dockerfile
