pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,js}"
      run: npx oxlint {staged_files}
    format:
      glob: "*.{ts,js,json}"
      run: npx oxfmt --check {staged_files}
    test:
      glob: "*.ts"
      run: npm test
